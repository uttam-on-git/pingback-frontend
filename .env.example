# ----------------------------------
# PingBack Frontend Environment Variables
# ----------------------------------
# This is an example file. Copy this to a new file named .env and fill in your actual values.
# The .env file is ignored by Git and should never be committed.

# The public URL of the backend API service.
# For local development, this should point to your local backend server.
# For production, this should point to your live Render URL.
# IMPORTANT: All variables exposed to the frontend MUST be prefixed with VITE_.
VITE_BACKEND_API_URL="http://localhost:3000"